# Bybit Expense Tracker — test build

A built copy of the front end, published so it can be deployed and tested. It
carries **no source, no specification and no credentials** — those live in the
private repository this build came from.

Nothing here talks to a server. The application is local-first: transactions,
settings and any Bybit credentials stay in an encrypted vault in your browser
(AES-256-GCM under PBKDF2-SHA256), and the host serves static files and learns
nothing.

## Trying it

1. Open the deployed URL.
2. Create a vault with a password. It protects local data only and cannot be
   reset, so use something you will remember.
3. **Настройки → Bybit Pay → Загрузить CSV-экспорт** with a Bybit Pay export, or
   the sample below.
4. Go back to **Операции**.

A sample file to paste into `sample.csv` if you do not have an export to hand:

```csv
Transaction ID,Time,Type,Status,Amount,Currency,Merchant,Fee,Fee Currency,Category
PAY-1,2026-08-18 09:15:00,Payment,Success,2500.00,RUB,Пятёрочка,0,RUB,grocery
PAY-2,2026-08-18 12:40:00,Payment,Success,234.99,RUB,Бургер Кинг,0,RUB,food
PAY-3,2026-08-17 20:00:00,Payment,Success,1290.00,RUB,Яндекс Такси,0,RUB,transport
PAY-4,2026-08-17 11:00:00,Refund,Success,-450.00,RUB,Ozon,0,RUB,shopping
PAY-5,2026-08-16 08:00:00,Transfer In,Success,50000.00,RUB,Свой счёт,0,RUB,
PAY-6,2026-08-15 19:30:00,Payment,Success,7800.00,RUB,Ozon,0,RUB,shopping
PAY-7,2026-08-12 14:00:00,Payment,Success,14600.00,RUB,Аренда,0,RUB,utilities
PAY-8,2026-08-10 09:00:00,Payment,Success,890.00,RUB,Кофемания,0,RUB,restaurant
```

Set the display currency to RUB in **Настройки → Отображение** first, or add a
rate under **Курсы валют** — an amount in a currency with no rate is left out of
the total and named in a banner rather than counted at a guessed rate.

## Connecting a Bybit Card key

**Настройки → Проверка соединения** first. It reports whether this browser can
actually read a response from `api.bybit.com`, which is the open question the
project has not been able to answer from a server in the US — Bybit blocks it
there. The answer decides whether a relay is needed at all, so the result of
that one tap is genuinely useful.

Then **Настройки → Bybit Card**. The key is checked before it is stored:

- A **Read Only** key connects.
- A key that can trade, transfer or withdraw is refused, and the refusal says
  which of those it found. Under it is **«Я принимаю риски, использовать этот
  ключ»** — the block is a recommendation, not a lock, and the choice is yours.
- If you take that option, the warning stays visible for as long as the key is
  connected, and it lists what the key can do. Change the key's permissions in
  Bybit afterwards and synchronisation stops until you confirm again: the
  consent covers the permissions you were shown, not the key forever.

A read-only key is still the right answer. The application holds the key in the
encrypted vault on your device, and its whole security argument is that a stolen
key is not worth stealing — which stops being true the moment the key can move
money.

## What to look at

The transfer on 16 August should **not** count towards spending, and the refund
on 17 August should reduce it. Re-importing the same file should add nothing.

## This is a preview

Regenerated by hand from the private repository, so it lags behind. Delete it
whenever it has served its purpose.
